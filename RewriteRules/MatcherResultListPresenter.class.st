"
I am a presenter that shows in a list all the times a rewrite rule had been matched with a block of code.
"
Class {
	#name : #MatcherResultListPresenter,
	#superclass : #SpListPresenter,
	#category : #'RewriteRules-Matcher'
}
